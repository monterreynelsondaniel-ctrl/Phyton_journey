def get_greet_user(greet):
 name = input("Enter your name: ")
 return greet + " " + name 

def get_number(number):
 num = int(number)
 if num % 2 == 0:
  return "your number is even"
 else:
  return "your number is odd"
  
def main():
  # firt greet the user
  greeting = get_greet_user("Hello")
  print(greeting)
  
  #show menu
  print("\nChoice a number: ")
  print("1. verify is a number is even or odd")  
  print("2. Exit")
  
  #user choice 
  choice = input("Enter a choice (1 or 2): ")
  if choice == "1":
      user_input = input("Enter a number: ")
      user_choice = int(user_input)
      result = get_number(user_input)
      print(f"Nice, {result}")
  elif choice == 2:
   print("Good bye") 
  else:
   print("invalid option")
  
main()